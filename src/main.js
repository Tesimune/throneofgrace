import { createApp } from 'vue'
import App from './App.vue'
import './index.css'
import 'flowbite'

// const app = createApp({});

// app.component('header', require('./components/layouts/Header.vue').default);

// app.mount('#app');


createApp(App).mount('#app')
